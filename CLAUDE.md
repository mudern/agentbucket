我要在这里写一个agentbucket,你先写前端页面，用react来写，最好是用tailwind来做样式。里面有两个logo。我希望左边一个贯通的栏。如果是管理员，里面
  就有部署agent,所有agent,然后点击每个agent卡片可以进入到里面，类似gpt/gemini/deepseek的对话。然后agent卡片上面要有状态（用个小绿点就够了我觉
  得），tag。然后这个页面支持搜索和按tag过滤。在下面是管理员权限控制，就是一共三种身份，超级管理员，管理员，普通用户，超级管理员多一个这个页面，
  就是可以决定谁是管理员。这个页面就是都有哪些注册用户，然后要不要把他变成管理员或者是普通用户，下一个页面是审批页面，这个是超级管理员和管理员都
  有的，就是可以审批普通用户的请求，主要是部署一个新agent的需求。还有申请可以使用某些ai token和鉴权token的请求，然后可以审批这些。然后下面两个页
  面是，可以新建ai token，然后大家就可以直接用这个ai token,比如说我可以准备deepseek token,glm token之类的。然后再下一个页面就是管理员可以新建一
  些其他鉴权用的token,如果agent创建的时候勾选了可以看到这些token,那么他们就可以通过skill看到这些token用来鉴权。大概就是这些。你去实现一下前端页
  面。

